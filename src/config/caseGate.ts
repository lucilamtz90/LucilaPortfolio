/** Client-side "soft" gate — a speed bump like Medium's paywall, not real security. */
export const CASE_GATE_PASSWORD = 'lucy-portfolio-2026';
export const CASE_GATE_SESSION_KEY = 'lucila-portfolio:case-unlocked';
